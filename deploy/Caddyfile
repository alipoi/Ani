# Caddy 反向代理 + 自动 HTTPS（可选，仅不用 Cloudflare 时使用）
# 默认部署走 Cloudflare CDN（Node 直连 :80），不需要 Caddy。
# 如用 Caddy：改好域名后 systemctl reload caddy；未备案域名可先去掉域名段直接用 IP 访问 :8080

ani.example.com {
    reverse_proxy 127.0.0.1:8080
    encode gzip
}
